<p id="notice"><%= notice %></p>
 
  <h1><%= @category.name %> </h1>
  <% if @posts.each do |post| %>
  <div class="post"> 
    <h3><%= link_to post.title, post %></h3>
    <p><%= post.body %></p>
    <div class="byline">
      <p class="meta">May 20, 2012 Posted by <a href="#">Someone</a></p>
      <p class="links"><a href="post.html">Read More</a>&nbsp;&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;&nbsp;<a href="#" title="b0x w">Comments</a></p>
    </div>
  </div>
  <%end.empty? %>
    <p> Sorry no post exists for this category</p>
  <% end %>
  <%= link_to 'Back', categories_path %>
